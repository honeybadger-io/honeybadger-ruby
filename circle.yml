machine:
  ruby:
    version: 2.2.3
dependencies:
  pre:
    - git submodule init
    - git submodule update
    - gem install bundler
  post:
    - bundle exec appraisal install
test:
  override:
    - bundle exec appraisal rspec
